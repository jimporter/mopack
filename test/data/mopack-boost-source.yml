packages:
  boost:
    origin: tarball
    url: https://archives.boost.io/release/1.89.0/source/boost_1_89_0.tar.bz2
    build:
      - type: custom
        outdir: null  # TODO: Remove this after v0.2 is released.
        build_commands:
          - >-
            ${{ host_platform == "windows" ?
                "bootstrap.bat" : "./bootstrap.sh"
            }} --with-libraries=iostreams
      - type: b2
        env:
          B2: ./b2
    linkage: path
