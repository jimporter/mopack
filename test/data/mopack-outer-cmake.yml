packages:
  greeter:
    source: directory
    path: greeter-cmake
    version: "1.0"
    build: cmake
    usage:
      type: path
      # XXX: Pass this via `include_path` instead?
      compile_flags: -I$srcdir/include
      library_path: .
